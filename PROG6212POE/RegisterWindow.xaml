<Window x:Class="PROG6212POE.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PROG6212POE"
        mc:Ignorable="d"
        Title="Register" Height="400" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="txtUsername"  Grid.Row="0" Margin="0,5" />
        <PasswordBox x:Name="txtPassword"  Grid.Row="1" Margin="0,5" />
        <ComboBox x:Name="cmbRole" Grid.Row="2" Margin="0,5">
            <ComboBoxItem Content="Lecturer" />
            <ComboBoxItem Content="Programme Coordinator" />
            <ComboBoxItem Content="Academic Manager" />
            <ComboBoxItem Content="HR" />
        </ComboBox>
        <Button Content="Register" Click="Register_Click" Grid.Row="3" Margin="0,20" Width="100" HorizontalAlignment="Center" />
        <TextBlock x:Name="lblMessage" Grid.Row="4" Foreground="Red" Margin="0,5" />
    </Grid>
</Window>
